-- A script that lists all record of the table second_table of db hbtn_0c_0
-- Result should be display in this order score and  name, order by score (top first)

SELECT score, name FROM second_table  ORDER BY score DESC;
