//Updates text color of <header> element to red
// script will be loaded in the head tag.

document.addEventListener('DOMContentLoaded', () => {
  const header = document.querySelector('header');
  header.style.color = '#FF0000';
});
